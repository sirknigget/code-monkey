---
phase: 01-build-project-librarian-agent-utilities
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - /Users/omergilad/workspace/AI/code-monkey/code_monkey/agents/project_librarian/utilities/code_parser.py
  - /Users/omergilad/workspace/AI/code-monkey/tests/test_code_parser.py
autonomous: true
must_haves:
  truths:
    - "parse_python_code() extracts class names from Python source"
    - "parse_python_code() extracts function names (sync and async) from Python source"
    - "parse_python_code() extracts import statements from Python source"
    - "parse_python_code() handles syntax errors gracefully (returns empty result)"
  artifacts:
    - path: "code_monkey/agents/project_librarian/utilities/code_parser.py"
      provides: "Python code structure extraction with ast"
    - path: "tests/test_code_parser.py"
      provides: "Tests for code parsing behavior"
  key_links:
    - from: "code_parser.py"
      to: "ast.NodeVisitor"
      via: "AST traversal for code structure"
---

<objective>
Create code parsing utility for the Project Librarian agent.

Purpose: Extract classes, functions, and imports from Python source files to understand code structure.

Output:
- code_monkey/agents/project_librarian/utilities/code_parser.py with parse_python_code() function
- tests/test_code_parser.py with test cases
</objective>

<execution_context>
@/Users/omergilad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omergilad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-build-project-librarian-agent-utilities/01-RESEARCH.md

Code parsing pattern from RESEARCH.md:
- Use ast.NodeVisitor to traverse AST
- Create dataclasses or NamedTuples for structured output
- Handle SyntaxError gracefully with try-except
- Visit ClassDef, FunctionDef, AsyncFunctionDef, Import, ImportFrom nodes
- Prefix async functions with "async " in output
</context>

<tasks>

<task type="auto">
  <name>Create code_parser.py with AST-based parsing</name>
  <files>code_monkey/agents/project_librarian/utilities/code_parser.py</files>
  <action>
Create code_monkey/agents/project_librarian/utilities/code_parser.py with:

1. ParsedCode NamedTuple with fields:
   - classes: list[str]
   - functions: list[str]
   - imports: list[str]

2. CodeExtractor class (ast.NodeVisitor subclass):
   - __init__ initializes empty lists for classes, functions, imports
   - visit_ClassDef: appends node.name, calls generic_visit
   - visit_FunctionDef: appends node.name, calls generic_visit
   - visit_AsyncFunctionDef: appends "async {node.name}", calls generic_visit
   - visit_Import: appends alias.name for each import
   - visit_ImportFrom: appends "{module}.{alias.name}" format (module may be None)

3. parse_python_code() function:
   - Parameter: source (str)
   - Returns: ParsedCode NamedTuple
   - Wraps ast.parse() in try-except for SyntaxError
   - Returns empty ParsedCode on error

4. Follow CONVENTIONS.md:
   - snake_case functions
   - Full type annotations
   - Docstrings for public functions
   - Import order: stdlib, then local

Reference patterns from 01-RESEARCH.md "Pattern 2: Code Structure Extraction with AST"
</action>
  <verify>python -c "from code_monkey.agents.project_librarian.utilities.code_parser import parse_python_code; print('Import successful')"</verify>
  <done>parse_python_code() function exists and can be imported</done>
</task>

<task type="auto">
  <name>Update utilities __init__.py to export code_parser</name>
  <files>code_monkey/agents/project_librarian/utilities/__init__.py</files>
  <action>
Add to utilities/__init__.py:
- Import and re-export parse_python_code from code_parser

Existing content should already have file_discovery import.
</action>
  <verify>python -c "from code_monkey.agents.project_librarian.utilities import parse_python_code; print('Export works')"</verify>
  <done>parse_python_code is importable via utilities module</done>
</task>

<task type="auto">
  <name>Write code parser tests</name>
  <files>tests/test_code_parser.py</files>
  <action>
Create tests/test_code_parser.py with pytest tests:

Test cases for valid Python:

1. test_extracts_class_names: Source with "class Foo:" and "class Bar:" -> classes=["Foo", "Bar"]
2. test_extracts_function_names: Source with "def foo():" and "def bar():" -> functions=["foo", "bar"]
3. test_extracts_async_functions: Source with "async def async_foo():" -> functions=["async async_foo"]
4. test_extracts_imports: Source with "import os" and "import sys" -> imports=["os", "sys"]
5. test_extracts_import_from: Source with "from pathlib import Path" -> imports=["pathlib.Path"]
6. test_extracts_all_structure: Source with mixed classes, functions, imports

Test cases for error handling:

7. test_syntax_error_returns_empty: Invalid Python source -> empty ParsedCode
8. test_empty_source_returns_empty: Empty string -> empty ParsedCode

Test source code directly with parse_python_code(source) function.
</action>
  <verify>uv run pytest tests/test_code_parser.py -v</verify>
  <done>All code parser tests pass</done>
</task>

</tasks>

<verification>
- Run: uv run pytest tests/test_code_parser.py -v
- Expected: All tests pass
</verification>

<success_criteria>
- parse_python_code() extracts classes, functions, and imports from Python source
- Async functions are prefixed with "async"
- Syntax errors are handled gracefully
- Tests verify all extraction behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-project-librarian-agent-utilities/01-02-SUMMARY.md`
</output>
