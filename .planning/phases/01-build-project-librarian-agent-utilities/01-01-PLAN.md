---
phase: 01-build-project-librarian-agent-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/omergilad/workspace/AI/code-monkey/code_monkey/agents/project_librarian/utilities/__init__.py
  - /Users/omergilad/workspace/AI/code-monkey/code_monkey/agents/project_librarian/utilities/file_discovery.py
  - /Users/omergilad/workspace/AI/code-monkey/tests/test_file_discovery.py
autonomous: true
must_haves:
  truths:
    - "discover_python_files() returns Python files from a directory"
    - "discover_python_files() excludes venv, .git, __pycache__, pytest_cache directories"
  artifacts:
    - path: "code_monkey/agents/project_librarian/utilities/file_discovery.py"
      provides: "File discovery with pathlib glob and directory exclusion"
    - path: "code_monkey/agents/project_librarian/utilities/__init__.py"
      provides: "Utility module exports"
    - path: "tests/test_file_discovery.py"
      provides: "Tests for file discovery behavior"
  key_links:
    - from: "file_discovery.py"
      to: "pathlib.Path.glob()"
      via: "glob pattern matching"
---

<objective>
Create file discovery utility for the Project Librarian agent.

Purpose: Enable discovering Python files across a project while excluding build artifacts, version control, and cache directories.

Output:
- code_monkey/agents/project_librarian/utilities/file_discovery.py with discover_python_files() function
- code_monkey/agents/project_librarian/utilities/__init__.py exporting the utility
- tests/test_file_discovery.py with test cases
</objective>

<execution_context>
@/Users/omergilad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omergilad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-project-librarian-agent-utilities/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md

File discovery pattern from RESEARCH.md:
- Use pathlib.Path.glob() for pattern matching
- Use frozenset for EXCLUDED_DIRS for efficient lookup
- Check f.parts to filter excluded directory names
- Return sorted list of Path objects
</context>

<tasks>

<task type="auto">
  <name>Create utilities directory and file_discovery.py</name>
  <files>code_monkey/agents/project_librarian/utilities/file_discovery.py</files>
  <action>
Create code_monkey/agents/project_librarian/utilities/ directory and file_discovery.py with:

1. EXCLUDED_DIRS constant as frozenset containing:
   - '.git', '.svn', '.hg' (version control)
   - 'venv', '.venv', 'env', '.env' (virtual environments)
   - '__pycache__', '.pytest_cache', '.tox', '.nox', '.mypy_cache' (Python cache)
   - 'node_modules', 'bower_components' (JS dependencies)
   - 'dist', 'build', '.egg-info' (build artifacts)

2. discover_python_files() function with signature:
   - Parameters: root (Path), pattern (str = "**/*.py")
   - Returns: list[Path] of discovered Python files
   - Implementation: Use root.glob(pattern), filter for files only, exclude any path containing excluded directories in parts
   - Return sorted list

3. All code must follow CONVENTIONS.md:
   - snake_case for functions
   - Full type annotations
   - PEP 8 style with 4-space indentation
   - Import organization: stdlib, then local
   - Docstrings for all public functions

Reference patterns from 01-RESEARCH.md "Pattern 1: File Discovery with pathlib"
  </action>
  <verify>ls -la code_monkey/agents/project_librarian/utilities/file_discovery.py && python -c "from code_monkey.agents.project_librarian.utilities.file_discovery import discover_python_files; print('Import successful')"</verify>
  <done>discover_python_files() function exists and can be imported</done>
</task>

<task type="auto">
  <name>Create utilities __init__.py</name>
  <files>code_monkey/agents/project_librarian/utilities/__init__.py</files>
  <action>
Create utilities/__init__.py that:
- Imports and re-exports discover_python_files from file_discovery
- Adds module docstring describing the utilities module
- Follows CONVENTIONS.md: no barrel file exports, explicit imports
</action>
  <verify>python -c "from code_monkey.agents.project_librarian.utilities import discover_python_files; print('Export works')"</verify>
  <done>Utility is importable via utilities module path</done>
</task>

<task type="auto">
  <name>Write file discovery tests</name>
  <files>tests/test_file_discovery.py</files>
  <action>
Create tests/test_file_discovery.py with pytest tests:

1. Create a temporary test directory structure with:
   - A Python file at root level
   - A Python file inside a subdirectory
   - A Python file inside .git/ subdirectory (should be excluded)
   - A Python file inside venv/ subdirectory (should be excluded)
   - A Python file inside __pycache__/ subdirectory (should be excluded)

2. Test cases:
   - test_discovers_python_files_at_root_and_nested: Verifies both files are found
   - test_excludes_git_directory: Verifies .git/ contents excluded
   - test_excludes_venv_directory: Verifies venv/ contents excluded
   - test_excludes_pycache_directory: Verifies __pycache__/ contents excluded
   - test_returns_sorted_list: Verifies files are sorted alphabetically
   - test_empty_directory_returns_empty_list: Edge case handling

Use pytest tmp_path fixture for test directory creation.
</action>
  <verify>uv run pytest tests/test_file_discovery.py -v</verify>
  <done>All file discovery tests pass</done>
</task>

</tasks>

<verification>
- Run: uv run pytest tests/test_file_discovery.py -v
- Expected: All tests pass
</verification>

<success_criteria>
- discover_python_files() function implemented with pathlib glob patterns
- Directory exclusion works for venv, .git, __pycache__, pytest_cache
- Tests cover discovery and exclusion behavior
- Code follows project conventions (type hints, docstrings, import order)
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-project-librarian-agent-utilities/01-01-SUMMARY.md`
</output>
