---
phase: 01-build-project-librarian-agent-utilities
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - /Users/omergilad/workspace/AI/code-monkey/code_monkey/agents/project_librarian/utilities/hash_utils.py
  - /Users/omergilad/workspace/AI/code-monkey/tests/test_hash_utils.py
autonomous: true
must_haves:
  truths:
    - "compute_file_hash() produces SHA-256 hash of any file"
    - "compute_file_hash() is deterministic (same file = same hash)"
    - "compute_file_hash() is collision-resistant (different files = different hash)"
    - "File change detection works (comparing hashes reveals changes)"
  artifacts:
    - path: "code_monkey/agents/project_librarian/utilities/hash_utils.py"
      provides: "SHA-256 file hashing for change detection"
    - path: "tests/test_hash_utils.py"
      provides: "Tests for hash computation behavior"
  key_links:
    - from: "hash_utils.py"
      to: "hashlib.file_digest()"
      via: "Streaming SHA-256 computation"
---

<objective>
Create hash computation utility for the Project Librarian agent.

Purpose: Compute SHA-256 hashes of files for change detection and caching.

Output:
- code_monkey/agents/project_librarian/utilities/hash_utils.py with compute_file_hash() function
- tests/test_hash_utils.py with test cases
</objective>

<execution_context>
@/Users/omergilad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/omergilad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-build-project-librarian-agent-utilities/01-RESEARCH.md

Hash utility pattern from RESEARCH.md:
- Use hashlib.file_digest() for efficient streaming (Python 3.11+)
- Takes file path (Path or str), returns hexdigest string
- Works with any file, not just Python files
- File is opened in binary mode ("rb")
</context>

<tasks>

<task type="auto">
  <name>Create hash_utils.py with SHA-256 hashing</name>
  <files>code_monkey/agents/project_librarian/utilities/hash_utils.py</files>
  <action>
Create code_monkey/agents/project_librarian/utilities/hash_utils.py with:

1. compute_file_hash() function:
   - Parameter: filepath (Path | str)
   - Returns: str (SHA-256 hexdigest)
   - Opens file in binary mode ("rb")
   - Uses hashlib.file_digest(f, "sha256") for efficient streaming
   - Returns digest.hexdigest()
   - Raises: Propagates OSError if file cannot be read

2. Follow CONVENTIONS.md:
   - snake_case function
   - Full type annotations (use Path | str union)
   - Docstring describing behavior

Reference patterns from 01-RESEARCH.md "Pattern 3: SHA-256 File Hashing"
</action>
  <verify>python -c "from code_monkey.agents.project_librarian.utilities.hash_utils import compute_file_hash; print('Import successful')"</verify>
  <done>compute_file_hash() function exists and can be imported</done>
</task>

<task type="auto">
  <name>Update utilities __init__.py to export hash_utils</name>
  <files>code_monkey/agents/project_librarian/utilities/__init__.py</files>
  <action>
Add to utilities/__init__.py:
- Import and re-export compute_file_hash from hash_utils

Existing content should already have file_discovery and code_parser imports.
</action>
  <verify>python -c "from code_monkey.agents.project_librarian.utilities import compute_file_hash; print('Export works')"</verify>
  <done>compute_file_hash is importable via utilities module</done>
</task>

<task type="auto">
  <name>Write hash utils tests</name>
  <files>tests/test_hash_utils.py</files>
  <action>
Create tests/test_hash_utils.py with pytest tests:

1. test_computes_sha256_hash: Create temp file with content, verify hash matches known value
   - Known SHA-256 for "hello world\n": 309ecc489c12d6eb4cc40f50c902f2b4d0ed77ee511a7c7a9bcd3ca86d4cd86f

2. test_hash_is_deterministic: Same file content produces same hash twice
3. test_different_content_different_hash: Two files with different content have different hashes
4. test_empty_file_hash: Empty file has specific hash (eikonix: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855)
5. test_raises_on_nonexistent_file: FileNotFoundError propagated for missing files
6. test_accepts_string_path: compute_file_hash works with str path, not just Path

Use pytest tmp_path for test file creation.
</action>
  <verify>uv run pytest tests/test_hash_utils.py -v</verify>
  <done>All hash utils tests pass</done>
</task>

</tasks>

<verification>
- Run: uv run pytest tests/test_hash_utils.py -v
- Expected: All tests pass
</verification>

<success_criteria>
- compute_file_hash() produces SHA-256 hash using efficient streaming
- Hashes are deterministic and collision-resistant
- Tests verify hash behavior for various file contents
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-project-librarian-agent-utilities/01-03-SUMMARY.md`
</output>
