# Quick Task 005: ProjectMapper Integration Test with Real LLM

**Completed:** 2026-02-02
**Duration:** ~25 minutes (7 tests with real LLM calls)
**Tasks:** 3/3 complete

## Summary

Created comprehensive integration tests for `ProjectMapper` using the real MiniMax LLM model and a realistic mock project (Python `requests` library).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create mock project folder | b6da143 | mock_project_folder/ |
| 2 | Create integration test | 51ee0d0 | tests/agents/project_librarian/test_project_mapper_real_llm.py |
| 3 | Fix test assertions | d0572fa | tests/agents/project_librarian/test_project_mapper_real_llm.py |

## Test Results

**7/7 tests passing:**

- `test_real_llm_fresh_scan` - Verifies full scan generates cache with realistic data
- `test_real_llm_incremental_update` - Verifies change detection on file modification
- `test_real_llm_specified_file_update` - Verifies `update(paths)` functionality
- `test_real_llm_generates_module_summaries` - Verifies `.codemonkey/_module.md` files
- `test_real_llm_cache_survives_reload` - Verifies cache persistence across instances
- `test_handles_new_subdirectory` - Verifies discovery of new directories
- `test_cache_contains_file_summaries` - Verifies file-level summaries in cache

## Deliverables

### mock_project_folder/
Cloned Python `requests` library with:
- 36 Python files across multiple modules
- Hierarchical package structure (`src/requests/`)
- Tests directory with test files
- Documentation structure

### tests/agents/project_librarian/test_project_mapper_real_llm.py
Integration tests using `get_minimax_model()` from `code_monkey/models/models.py`:
- Fresh scan verification
- Incremental update behavior
- Specified file updates
- Module summary generation
- Cache persistence
- New directory discovery

### .codemonkey cache (generated)
After running tests, cache contains:
- `file_hashes.json` - Hash values for all Python files
- `project_context.json` - LLM-generated project overview
- 43 `.md` files - Module and file summaries generated by LLM

## Cache Structure Verified

```
.codemonkey/
├── file_hashes.json          # Hash cache for change detection
├── project_context.json      # {"context": "..."} LLM-generated overview
├── setup.md                  # Root module summary
├── _module.md                # Root module summary
├── src/
│   ├── _module.md
│   └── requests/
│       ├── _module.md
│       ├── __init__.md
│       ├── models.md
│       ├── sessions.md
│       └── ... (15 more file summaries)
├── tests/
│   ├── _module.md
│   └── ... (test file summaries)
└── docs/
    ├── _module.md
    └── ... (doc file summaries)
```

## Deviations from Plan

**None** - Plan executed exactly as written.

## Usage

Run integration tests:
```bash
uv run pytest tests/agents/project_librarian/test_project_mapper_real_llm.py -v
```

Run single test:
```bash
uv run pytest tests/agents/project_librarian/test_project_mapper_real_llm.py::TestProjectMapperRealLLM::test_real_llm_fresh_scan -v
```

## Notes

- Tests use `get_minimax_model()` which connects to MiniMax API
- Each test cleans up `.codemonkey` cache before/after execution
- Full test suite takes ~22 minutes due to real LLM API calls
- Mock project folder uses `requests` library as specified (well-known, hierarchical structure)
